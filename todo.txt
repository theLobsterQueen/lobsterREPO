Allow for graceful object instantiation
Fix non-global translations in regard to how they process rotations
Create project-directory script, which can create new directories configured to work
	in the best possible way with the engine
Create options for resizing the Window
Separate out the scene viewport and the UI panels to the sides, have SDL
	render only to the viewport
Add collision detection system
Improve editor so that it can draw things like Transform modification arrows,
	highlight the boxes of selected items, etc.
Create build dialog for exporting projects
Fix Windows cross-compilation again
Add end-user UI capabilities 
